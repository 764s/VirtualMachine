
## **指令信息**

`指令名 [Info]`
- `Info` 为需求内容, 如果未输入则默认无
- `file` = `Docs/VM_Summary.md`

## **执行流程**

严格按以下顺序执行：

1. **阅读环境**：阅读`file`，理解当前任务环境
2. **确认状态**：阅读`file`的串行计划列表，梳理当前子任务的执行状态
3. **处理需求**：(如果需求无内容,跳过此步), 基于前三步获取的【最新上下文】，处理 `info`。
4. **总结产出**：
  - 输出 `file` 链接
  - 总结当前状态

